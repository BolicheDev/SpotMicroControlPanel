import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'spotmicro_control_panel',
  webDir: 'build'
};

export default config;
